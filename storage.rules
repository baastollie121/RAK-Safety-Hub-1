rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to write (upload) files.
    // Allow public read access to all files so they can be displayed in the app.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
